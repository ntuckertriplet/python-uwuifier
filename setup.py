from setuptools import setup
setup(
    name='uwuifier',
    version='0.1.0',
    description='because I should not code tired',
    author='Nathan Tucker',
    license='MIT',
    packages=['uwuifier']
)
